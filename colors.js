const colors= require('colors');
console.log("hello this is now working and now I know how things".green);

// Example of syncronus programming language
setTimeout(() => {
    console.log("Node is Asynchromus language");
}, 2000);